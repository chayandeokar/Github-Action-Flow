on:
  push:
    branches:
      - master
      - dev
  pull_request:
    branches:
      - master
jobs:
  helloworld:
    runs-on: ubuntu-latest
    steps: 
      - name: "echo Basic info"
        run: echo "hello this is multi_event"
